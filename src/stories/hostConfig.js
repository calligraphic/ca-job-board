/** @format
 *
 *
 * @since 0.0.1
 * @module config/.storybook/config
 * @see module:storybook
 */
const hostConfig = {
  mobXDevTools: true,
  title: 'A host container for components under test.',
  hr: true,
  align: 'center bottom', // left-center-right and top-middle-bottom
  height: '80%',
  width: 400,
  background: 'yellow',
  backdrop: 'white',
  cropMarks: true,
  border: '0px',
  padding: '5px'
};

export default hostConfig;
