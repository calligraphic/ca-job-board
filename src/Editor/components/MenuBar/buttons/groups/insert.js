import image from '../image';
import footnote from '../footnote';
// import hr from '../hr';
import table from '../table';

const insert = {
  image,
  footnote,
  // hr,
  table,
};

export default insert;
