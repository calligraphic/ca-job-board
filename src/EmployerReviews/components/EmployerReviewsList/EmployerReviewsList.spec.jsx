/** @format */

// TODO: Enforce 'use strict';

import React from 'react';
import { shallow } from 'enzyme';
import EmployerReviewsList from './EmployerReviewsList';

it('renders without crashing', () => {
  shallow(<JobPosting />);
});
